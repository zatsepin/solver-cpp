#ifndef DEFS_H
#define DEFS_H

namespace solver
{

typedef unsigned long ulong;

namespace constants
{
	const ulong DEFAULT_DESK_SIZE = 9;

	const ulong MIN_CELL_VALUE = 1;

	const ulong MAX_CELL_VALUE = 9;
} // namespace constants

} // namespace solver

#endif // DEFS_H